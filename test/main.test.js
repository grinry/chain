const { expect } = require('chai');

describe('I don\'t know', () => {
    it('should call something good.', () => {

        const sum1 = 12;
        const sum2 = 12;

        expect(sum2).to.be.equal(sum1);

    });
});
